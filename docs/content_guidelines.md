# Content Guidelines

This repository uses the project name `Believe in Me`.

Keep the repository focused on the sample engine. Avoid adding:

- production assets
- long-form product notes
- generated analysis outputs
- local credential paths
- large data files
- copied material from unrelated repositories

The model should remain compact and generic. If a detail makes the repository
harder to understand as a standalone engine example, leave it out.
